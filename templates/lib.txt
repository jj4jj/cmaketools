
aux_source_directory(${PROJECT_SOURCE_DIR}/base cur_aSRCS)
set(cur_SRCS "${cur_aSRCS}")

<extra_srcs>

include_directories(
${PROJECT_SOURCE_DIR}
<includes>
/usr/include)

add_library(<lib_name> <lib_type> ${cur_SRCS})

link_directories(
<linkpaths>
/usr/local/lib)

target_link_libraries(<lib_name>
<linklibs>)

